# ── AI Agent (n8n_ai_agent_node.py) ─────────────────────────────────────────
openai>=1.0.0                        # OpenAI, Azure, and Ollama-compatible backends
anthropic>=0.25.0                    # Anthropic Claude backend
google-generativeai>=0.8.0           # Gemini backend (native SDK, required for search grounding)

# ── Google Drive (google_drive_api.py) ──────────────────────────────────────
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0

# ── HTTP Request (https_request_node.py) ────────────────────────────────────
httpx>=0.27.0                        # primary HTTP transport (sync + async)

# ── Web Search Tool (web_search_tool.py) ────────────────────────────────────
# No extra packages — uses httpx via https_request_node.py
# Pick one search provider and supply its API key:
#   Tavily  → https://tavily.com           (AI-focused, generous free tier)
#   Brave   → https://api.search.brave.com
#   SerpAPI → https://serpapi.com

# ── n8n Merge Node (n8n_merge_node.py) ──────────────────────────────────────
# no third-party dependencies
